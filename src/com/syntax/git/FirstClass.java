package com.syntax.git;

public class FirstClass {
	/*
	 * what is git ?  It's a source code management 
	 * git is a version control tool and is also called a local repository
	 * git works by managing and storing new features to our program 
	 * git is designed to understand linux style comands
	 * 
	 * 
	 * git bash= is what windows uses to let us comunicate with git though comands 
	 * for mac it's git terminal
	 * 
	 * 
	 * 
	 * what is a version?
	 * A version is an additional features to previous software 
	 * latest changest made in a software program
	 * 
	 * what is github?
	 * git hub is a remote repository which stores your program verisions 
	 * a copy of your local repository in the cloud 
	 * 
	 * 
	 * how can we use git hub when we are working in teams?
	 * if three people working in one user story 
	 * we can work on the same user story remotely using git hub
	 * advantage of git hub it allows you to colaborate with friends remotely
	 * 
	 * what is a terminal?
	 * in window we call it comand prompt
	 * 
	 * why do we use terminals?
	 * when we want to comunicate with our computer
	 * 
	 * how can we comunicate with git through our computer?
	 * by gitbash this will allow us to tell we want to use our local repository
	 * 
	 * 
	 * Windows Commands
	 * 
	 * pwd= present current directory
	 * a slash / represents gaps between the folders 
	 * ls= list of all files that are in your current directory
	 * cd= current directory
	 * cd..=moves a step back
	 * cd ~= navigates you to home page
	 * git init= initializes repository
	 * 
	 * 
	 * 
	 * 
	 * 
	 */

}
