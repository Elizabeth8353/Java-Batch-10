package com.syntax.notes;

public class EqualsOperator {

	public static void main(String[] args) {
		/*
		 * == operator only works with primitive data types can work with strings but not recommended to be used to compare strings
		 * == checks if both obj points to the same memory location 
		 * 
		 * for non primitive types we should use ".equals" methods
		 * .equals evaluates to the comparison of values in the obj not just the memory location
		 * 
		 * 
		 */
		
				

	}

}
